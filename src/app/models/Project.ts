export class Project {
    title: string;
    src: string;
    elementId: string

    constructor(title: string, src: string, elementId:string) {
        this.title = title;
        this.src = src;
        this.elementId = this.elementId;
    }
}