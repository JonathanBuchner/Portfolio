import { VideoBackgroundDirective } from './video-background.directive';

describe('VideoBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
