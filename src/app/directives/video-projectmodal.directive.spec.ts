import { VideoProjectmodalDirective } from './video-projectmodal.directive';

describe('VideoProjectmodalDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoProjectmodalDirective();
    expect(directive).toBeTruthy();
  });
});
